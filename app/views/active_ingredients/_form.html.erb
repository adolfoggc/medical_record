<%= form_with(model: active_ingredient) do |form| %>
  <% if active_ingredient.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(active_ingredient.errors.count, "error") %> prohibited this active_ingredient from being saved:</h2>

      <ul>
        <% active_ingredient.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :common_use, style: "display: block" %>
    <%= form.text_field :common_use %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
